[Service]
# Opt-in public administration endpoint. Keep a different random admin.token
# on every server and protect TCP/9797 with the host firewall.
Environment=MOUSEVPN_ADMIN_LISTEN=0.0.0.0:9797
Environment=MOUSEVPN_ADMIN_ALLOW_PUBLIC=1
