# Install as /etc/sysctl.d/99-mousevpn-forwarding.conf on the VPN server.
net.ipv4.ip_forward = 1

# IPv6 is intentionally not forwarded until the protocol supports it end to end.
net.ipv6.conf.all.forwarding = 0
